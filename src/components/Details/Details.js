import React from 'react';
import Header from '../Header/Header';

const Details = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is details</h1>
        </div>
    );
};

export default Details;